int f();
int a=10;

 int main(){
    int i;
    int x=strtol(a,NULL,16);
    char a[30]="hello world";
    i=a[10];
    f(x);
    x=x-9;
    x=a+b*(1+3);
    if(a>3) a=a+1;
    else a=a-1;
    for(i=0;i<10&&a<9||b>3;i=i+1) ;
    switch (a*10)
    {
    case d:
        /* code */
        break;
    case e:
    default:
        printf("hello",world);
        break;
    }
    return 0;
}